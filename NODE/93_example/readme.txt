회원가입을 하지 안ㄹ고도 사용할 수 있는 간단한 방명록 웹 서비스

몽고 컬렉션 .. visitors
    GET /write 일때는 아래의 정보를 입력해서 전송 할 수 있는 form
    방문자, 글 비밀번호, 코멘트
    text    password    textarea

    POST /write는 위 form을 전달받을 url
    사용자로부터 전달받은 데이터를 토대로 문서를 만드는데
    하나를 추가하는 데 (방문일자(date) Date.now(), new Date()) insert

    GET /list 는 컬렉션에서 데이터 가지고 와서 최대한 아름답게 렌더링

//---------------------------------------------------------
[수정]
    1#
    /list 에서 수정을 요구하는 사용자가 /update로 문서 ID값을 가지고 이동할 수 있게 처리 해둠.

    2#
    /update로 요청이 들어왔을 때 같이 전달받은 ID에 해당하는 문서를 찾아와서 (원문내용을 보여주기위해)
    update.ejs로 렌더링을 함 (update ejs파일은 만들어야하고, write.ejs를 가져와서 수정)

    3#
    update.ejs파일은 렌더링 될때 전달받은 문서 내용을 value로 설정해서 사용자가 원문을 볼 수 있게 유도하고
    사용자의 submit에 의해 /update (post)로 제출될수 있게 form설정

    4#
    /update (POST)로 요청이 들어왔을때 전달받은 body내용을 토대로 디비 업데이트 처리

    ???
    생각해볼 부분? 수정과 삭제를 요구하는 사용자에게 비밀번호를 입력받아 처리하고자 한다면?
    창을 1개더 만드는게낫다